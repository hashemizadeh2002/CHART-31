# CHART-31
```mermaid
graph TD
    subgraph UDEs [اثرات نامطلوب]
        A["هزینه حقوق و دستمزد به ازای سفارش (Payroll CPO) بالاست"]
        B["نیاز مکرر به اضافه‌کاری برای اتمام کارهای روزانه"]
        C["فرسودگی شغلی و افزایش نرخ خروج نیروهای خوب"]
    end

    subgraph Causes [علل]
        D("بهره‌وری کلی (IPH) پایین‌تر از حد انتظار است")
        E("زمان‌های تلف‌شده و بدون ارزش افزوده (مانند انتظار) در فرآیند زیاد است")
        F("جریان کار بین مراحل مختلف (مثلا بین جمع‌آوری و بسته‌بندی) نامتوازن است")
        G("سرپرستان دید لحظه‌ای و دقیقی از عملکرد افراد و گلوگاه‌ها ندارند")
        
        subgraph RootCauses [علل ریشه‌ای]
            RC1["سیستم تخصیص کار 'فشاری' (Push) است و باعث انباشت کار قبل از گلوگاه و انتظار بعد از آن می‌شود"]
            RC2["فقدان یک سیستم مدیریت عملکرد لحظه‌ای برای ارائه بازخورد فوری به نیروها و سرپرستان"]
        end
    end

    D --> A
    D --> B
    B --> C
    
    E --> D
    F --> E
    G --> F
    
    RC1 --> F
    RC2 --> G
